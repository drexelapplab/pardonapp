//The models for a User(can be edited if you want to add things like email, phone number, etc upon signup)
export class User {
    id: number;
    username: string;
    password: string;
    firstName: string;
    lastName: string;
}
